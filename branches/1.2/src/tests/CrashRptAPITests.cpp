#include "stdafx.h"
#include "Tests.h"
//#include "CrashRpt.h"

REGISTER_TEST(Test_crInstall);

void Test_crInstall()
{ 
  TEST_ASSERT(0);
  // Test crInstallW with zero info
  /*{
    CR_INSTALL_INFOW infoW;
    memset(&infoW, 0, sizeof(CR_INSTALL_INFOW));
    infoW.cb = sizeof(CR_INSTALL_INFOW);

    int nResult = crInstallW(&infoW);
  }*/
}